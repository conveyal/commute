import {connect} from 'react-redux'

import CommuterGroup from '../components/commuter-group'

function mapStateToProps (state) {
  return {}
}

export default connect(mapStateToProps)(CommuterGroup)
